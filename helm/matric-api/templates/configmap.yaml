apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "matric-api.fullname" . }}-config
  labels:
    {{- include "matric-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}